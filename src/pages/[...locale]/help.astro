---
import HelpSection from "../../components/index-page/HelpSection.astro";
import type { Lang } from "../../i18n/utils";
import { getLocaleStaticPaths, useTranslations } from "../../i18n/utils";
import BaseLayout from "../../layouts/BaseLayout.astro";

export const getStaticPaths = getLocaleStaticPaths;

interface Props {
	lang: Lang;
}

const { lang } = Astro.props;
const t = useTranslations(lang);
---

<BaseLayout title={t("page.help.title")} description={t("page.help.description")} lang={lang}>
	<HelpSection titleTag="h1" lang={lang} />
</BaseLayout>
